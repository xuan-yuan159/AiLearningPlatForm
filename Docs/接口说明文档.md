# 接口说明文档

本文档用于说明 AI 助教在线学习平台的后端接口协议。

## 全局状态码说明

| 状态码 | 说明 |
| :--- | :--- |
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 401 | 未授权或 Token 已过期 |
| 500 | 服务器内部错误 |

---

## Knife4j 接口文档

本项目集成了 Knife4j，提供了更美观、易用的接口文档界面。

### 访问地址

| 服务 | 端口 | 访问地址 |
| :--- | :--- | :--- |
| **全局api接口 (gateway-service)** | 8080 | `http://localhost:8080/doc.html` |
| **认证服务 (auth-service)** | 8081 | `http://localhost:8081/doc.html` |
| **课程管理服务 (course-service)** | 8082 | `http://localhost:8082/doc.html` |

### 调试说明

1. **界面功能**：左侧菜单可以按模块查看接口，支持搜索和离线导出文档。
2. **在线调试**：在接口详情页切换到“调试”页签，填入参数即可发送请求。
3. **全局参数**：可以在右上角的“个性化设置”或“全局参数设置”中统一配置 `Authorization` 请求头。

## 1. 认证模块 (Auth Module)

### 1.1 用户注册

**接口地址**：`/auth/register`

**请求方式**：`POST`

**接口描述**：新用户注册账号。默认注册身份为学生。

#### **请求参数 (JSON)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| username | String | 是 | 用户名（唯一） |
| password | String | 是 | 密码 |
| identity | Integer | 否 | 身份（0-学生，1-教师），默认为 0 |
| nickname | String | 否 | 昵称 |
| email | String | 否 | 邮箱 |
| phone | String | 否 | 手机号 |

**请求示例**：
```json
{
    "username": "testuser",
    "password": "password123",
    "identity": 0,
    "nickname": "小明",
    "email": "test@example.com"
}
```

#### **响应参数 (JSON)**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| code | Integer | 状态码（200 表示成功） |
| message | String | 提示信息 |
| data | Object | 响应数据主体（注册成功为 null） |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---

### 1.2 用户登录

**接口地址**：`/auth/login`

**请求方式**：`POST`

**接口描述**：用户通过用户名和密码进行身份验证。验证成功后，系统将返回用于后续请求鉴权的 JWT Token。

#### **请求参数 (JSON)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| username | String | 是 | 用户名 |
| password | String | 是 | 密码 |

**请求示例**：
```json
{
    "username": "admin",
    "password": "password"
}
```

#### **响应参数 (JSON)**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| code | Integer | 状态码（200 表示成功） |
| message | String | 提示信息 |
| data | Object | 响应数据主体 |
| └─ token | String | 身份验证令牌 (JWT) |
| └─ userId | Long | 用户唯一标识 ID |
| └─ role | String | 用户角色 (如: STUDENT, TEACHER, ADMIN) |

**响应示例 (成功)**：
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "token": "eyJhbGciOiJIUzUxMiJ9...",
        "userId": 100,
        "role": "ADMIN"
    }
}
```

**响应示例 (失败)**：
```json
{
    "code": 400,
    "message": "Username and password are required",
    "data": null
}
```

---

### 1.3 用户登出

**接口地址**：`/auth/logout`

**请求方式**：`POST`

**接口描述**：注销当前用户的登录状态，并清除服务端（Redis）中的 Token 缓存。

**请求头要求**：
- `Authorization`: `Bearer {token}`

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---

## 2. 课程管理模块 (Course Module)

### 2.1 创建课程

**接口地址**：`/course`

**请求方式**：`POST`

**接口描述**：教师创建课程草稿。

#### **请求参数 (JSON)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| title | String | 是 | 课程标题 |
| description | String | 否 | 课程简介 |
| coverUrl | String | 否 | 封面URL |
| category | String | 否 | 分类 |
| tags | String | 否 | 标签(逗号分隔) |
| difficulty | Integer | 否 | 难度(1-入门 2-进阶 3-高级) |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": 1001
}
```

---

### 2.2 发布课程

**接口地址**：`/course/{id}/publish`

**请求方式**：`POST`

**接口描述**：将课程状态变更为已发布。

#### **请求参数 (路径)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 课程ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---

### 2.3 获取课程分页列表

**接口地址**：`/course/list`

**请求方式**：`GET`

**接口描述**：按分类或关键字搜索课程列表，支持分页与缓存。

#### **请求参数 (Query)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| page | Integer | 否 | 页码 (默认1) |
| size | Integer | 否 | 每页条数 (默认10) |
| category | String | 否 | 分类筛选 |
| keyword | String | 否 | 关键字筛选 |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "records": [...],
        "total": 100,
        "size": 10,
        "current": 1
    }
}
```

---

### 2.4 编辑课程

**接口地址**：`/course/{id}`

**请求方式**：`PUT`

**接口描述**：修改课程信息。

#### **请求参数 (路径 + JSON)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 课程ID (路径参数) |
| title | String | 是 | 课程标题 |
| description | String | 否 | 课程简介 |
| coverUrl | String | 否 | 封面URL |
| category | String | 否 | 分类 |
| tags | String | 否 | 标签(逗号分隔) |
| difficulty | Integer | 否 | 难度(1-入门 2-进阶 3-高级) |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---

### 2.5 下架课程

**接口地址**：`/course/{id}/unpublish`

**请求方式**：`POST`

**接口描述**：将课程状态变更为已下架。

#### **请求参数 (路径)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 课程ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---

### 2.6 删除课程

**接口地址**：`/course/{id}`

**请求方式**：`DELETE`

**接口描述**：软删除课程。

#### **请求参数 (路径)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 课程ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---

### 2.7 获取课程详情

**接口地址**：`/course/{id}`

**请求方式**：`GET`

**接口描述**：获取课程基本信息。

#### **请求参数 (路径)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 课程ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "id": 1001,
        "title": "测试课程",
        "description": "...",
        "coverUrl": "...",
        "category": "后端",
        "tags": "java,spring",
        "difficulty": 1,
        "status": 1,
        "publishedAt": "2026-04-03T18:00:00",
        "createdAt": "2026-04-03T18:00:00",
        "updatedAt": "2026-04-03T18:00:00"
    }
}
```

---

## 3. 章节管理模块 (Chapter Module)

### 3.1 创建章节

**接口地址**：`/chapter`

**请求方式**：`POST`

**接口描述**：在指定课程下创建新章节。

#### **请求参数 (JSON)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| courseId | Long | 是 | 课程ID |
| title | String | 是 | 章节标题 |
| summary | String | 否 | 章节简介 |
| sortOrder | Integer | 是 | 排序 |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": 1
}
```

---

### 3.2 编辑章节

**接口地址**：`/chapter/{id}`

**请求方式**：`PUT`

**接口描述**：修改章节标题、简介、排序等。

#### **请求参数 (路径 + JSON)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 章节ID (路径参数) |
| courseId | Long | 是 | 课程ID |
| title | String | 是 | 章节标题 |
| summary | String | 否 | 章节简介 |
| sortOrder | Integer | 是 | 排序 |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---

### 3.3 删除章节

**接口地址**：`/chapter/{id}`

**请求方式**：`DELETE`

**接口描述**：删除指定的章节。

#### **请求参数 (路径)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 章节ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---

### 3.4 获取章节列表

**接口地址**：`/chapter/list/{courseId}`

**请求方式**：`GET`

**接口描述**：查询指定课程的章节目录（按排序）。

#### **请求参数 (路径)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| courseId | Long | 是 | 课程ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": [
        {
            "id": 1,
            "courseId": 1001,
            "title": "第一章 基础",
            "summary": "...",
            "sortOrder": 1
        }
    ]
}
```

---

## 4. 资源管理模块 (Resource Module)

### 4.1 绑定资源到章节

**接口地址**：`/resource/{id}/bind/{chapterId}`

**请求方式**：`POST`

**接口描述**：将上传好的视频或资料挂载到特定章节。

#### **请求参数 (路径)**

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| id | Long | 是 | 资源ID |
| chapterId | Long | 是 | 章节ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "success",
    "data": null
}
```

---


